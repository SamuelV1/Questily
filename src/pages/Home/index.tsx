import React from 'react'
import { useData } from '../../hooks/datagrabbler'

export default function Home() {
const info = useData("merlin")

    return (
        <div>
            
        </div>
    )
}
